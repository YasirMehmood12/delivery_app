import 'package:flutter/material.dart';

class DecemberModel{
  final String? upText;
  final String? downText;
  final String? rightText;
  final Color? color;
  final Color? colorText;

  DecemberModel({this.color,this.colorText,
    this.upText, this.downText, this.rightText});
}
class WithdrawModel{
  final String? upText;
  final String? downText;
  final String? rightText;
  final Color? color;
  final Color? colorText;

  WithdrawModel({this.color,this.colorText,
    this.upText, this.downText, this.rightText});
}